# Configuración de Ejemplo para Render
# Copia este archivo a .env y completa los valores

# 1. FLASK
FLASK_ENV=production
FLASK_DEBUG=False

# 2. RENDER (Render lo establece automáticamente)
# RENDER=true

# 3. API KEY GEMINI (Opcional - para dashboard)
# Si quieres proteger la API key, copia aquí tu clave de Gemini
# Ve a: https://makersuite.google.com/app/apikeys
# GEMINI_API_KEY=tu_api_key_aqui

# 4. DATABASE (Opcional - para PostgreSQL en producción)
# Si usas PostgreSQL en Render en lugar de SQLite:
# DATABASE_URL=postgresql://usuario:password@host:puerto/database

# 5. PUERTO (Render lo establece automáticamente en 10000)
# PORT=10000
